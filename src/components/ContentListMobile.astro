---
import ContentCard from "./ContentCard.astro";

interface Props {
  data: {
    contentId: string;
    type: string;
    title: string;
    description: string;
    contentUrl: string;
    genre: string;
    duration: string;
    thumbnailUrl: string;
  }[];
  title: string;
}

const { data, title } = Astro.props;
---

<div class="px-4 md:px-12 mt-5 space-y-8 w-96 h-1">
  <div>
    <p
      class="text-white text-lg lg:text-md md:text-xl lg:text-2xl font-semibold mb-4"
    >
      {title}
    </p>

    <div
      class="grid lg:grid-cols-4 grid-cols-3 lg:gap-2  lg:overflow-visible h-1"
    >
      {data.map((content) => <ContentCard data={content} />)}
    </div>
  </div>
</div>
